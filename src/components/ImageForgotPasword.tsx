import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ImageForgotPasword = ({img}:{img:JSX.Element}) => {
  return (
    <View>
      {img}
    </View>
  )
}

export default ImageForgotPasword

const styles = StyleSheet.create({})